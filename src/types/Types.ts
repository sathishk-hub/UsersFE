export interface IUserDetail {
    email?: string,
    firstName?: string,
    lastName?: string,
    phoneNumber?: string,
    dob?: string,
    location?: string,
    degree?: string,
    password?: string
}